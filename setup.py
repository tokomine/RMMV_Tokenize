from setuptools import setup, find_packages

setup(
    name='rmmv_tokenize',
    version='0.1.0',
    description='A Python package for RMMV tokenization',
    author='',
    author_email='',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.11',
)
